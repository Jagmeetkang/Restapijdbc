CREATE TABLE PRODUCT (PRODUCTID INT PRIMARY KEY ,
                       SKU VARCHAR(255) ,
                       NAME VARCHAR(255),
                       DESC VARCHAR(255),
                       PRICE VARCHAR(255) );